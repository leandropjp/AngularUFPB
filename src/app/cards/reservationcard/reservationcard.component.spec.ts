/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ReservationcardComponent } from './reservationcard.component';

describe('Component: Reservationcard', () => {
  it('should create an instance', () => {
    let component = new ReservationcardComponent();
    expect(component).toBeTruthy();
  });
});
